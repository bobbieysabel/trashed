{% set navPages = collections.all | eleventyNavigation %}
<nav>
    <ul>
        {%- for entry in navPages %}
            <li{% if entry.url == page.url %} class="my-active-class" {% endif %}>
                <a href="{{ entry.url | url }}" class="nav">{{ entry.title }}</a>
            </li>
        {%- endfor %}
    </ul>
</nav>